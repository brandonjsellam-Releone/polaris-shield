# Python build / cache artifacts
__pycache__/
*.pyc
.mypy_cache/
.pytest_cache/
.venv/

# Ephemeral LMS release-signing outputs (fresh one-time key per run; NEVER commit the spent .prv)
release/release/

# Generated/transient release manifests (regenerated by make bundle; signed in CI)
release/RELEASE_MANIFEST.json
release/RELEASE_MANIFEST.sha256
sbom/sbom.full.cdx.json

# OS
.DS_Store
